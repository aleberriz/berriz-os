---
/**
 * Field note: "Gen AI without data is a house on stilts"
 * → /field-notes/gen-ai-without-data/
 *
 * Its own look, and deliberately neither of the other two bare notes: a
 * harbour cutaway. The page is split by a waterline that runs through every
 * section, sky above and dark water below, with Archivo Black caption type
 * borrowed from the meme it opens with.
 *
 * The hero drawing is original work, drawn here in SVG rather than borrowed
 * from the circulating "AI FIRST / DATA FIRST" meme, whose author is unknown.
 * Same joke, our own pencil.
 *
 * Inter is already loaded site-wide (ThemeHead.astro); only Archivo and
 * Archivo Black are imported here.
 */
---

<div class="gnd">
  <div class="gnd-inner">
    <!-- ============ HERO ============ -->
    <section class="gnd-hero">
      <p class="gnd-kicker">Field note &middot; AI and the semantic layer</p>
      <h1>&ldquo;AI first&rdquo; is a house on stilts.</h1>
      <p class="gnd-dek">
        An agent needs to know what your data means, how it fits together, and
        when to trust it. Choosing a model is only part of that work.
      </p>

      <!-- ============ THE DRAWING ============ -->
      <figure class="gnd-meme">
        <div class="gnd-panels">
          <!-- ---- panel A: AI first ---- -->
          <svg viewBox="0 0 260 330" role="img" aria-labelledby="gnd-a">
            <title id="gnd-a"
              >Cutaway of a large ornate building sitting above the waterline
              on thin crossed piles. Several piles are snapped and none of them
              reach the rock below.</title
            >
            <rect x="0" y="0" width="260" height="150" class="g-sky" />
            <rect x="0" y="150" width="260" height="180" class="g-water" />

            <!-- the building -->
            <g transform="rotate(-4 130 150)">
              <rect x="38" y="46" width="172" height="104" class="g-wall" />
              <rect x="146" y="18" width="64" height="30" class="g-wall" />
              <rect x="38" y="40" width="172" height="8" class="g-trim" />
              <!-- rooftop pool and parasols -->
              <rect x="56" y="26" width="52" height="14" class="g-pool" />
              <path d="M120 40 V24" class="g-thin" />
              <path d="M107 24 L133 24 L120 11 Z" class="g-para" />
              <path d="M158 18 V13" class="g-thin" />
              <path d="M150 13 L166 13 L158 7 Z" class="g-para" />
              <!-- windows -->
              <g class="g-win">
                <rect x="50" y="58" width="16" height="20" />
                <rect x="74" y="58" width="16" height="20" />
                <rect x="98" y="58" width="16" height="20" />
                <rect x="122" y="58" width="16" height="20" />
                <rect x="146" y="58" width="16" height="20" />
                <rect x="170" y="58" width="16" height="20" />
                <rect x="50" y="88" width="16" height="20" />
                <rect x="74" y="88" width="16" height="20" />
                <rect x="122" y="88" width="16" height="20" />
                <rect x="146" y="88" width="16" height="20" />
                <rect x="170" y="88" width="16" height="20" />
                <rect x="50" y="118" width="16" height="20" />
                <rect x="98" y="118" width="16" height="20" />
                <rect x="146" y="118" width="16" height="20" />
                <rect x="170" y="118" width="16" height="20" />
              </g>
              <!-- cracks -->
              <path d="M96 88 l8 14 -7 10 9 16 -6 12" class="g-crack" />
              <path d="M188 46 l-6 18 8 14 -5 20" class="g-crack" />
            </g>

            <!-- piles: crossed, snapped, short of rock -->
            <g class="g-pile">
              <path d="M52 150 L86 236" />
              <path d="M96 150 L58 230" />
              <path d="M112 150 L140 244" />
              <path d="M170 150 L146 214" />
              <path d="M158 226 L150 252" />
              <path d="M196 150 L214 218" />
              <path d="M214 232 L206 258" />
            </g>
            <!-- loose rubble on the bed -->
            <g class="g-rubble">
              <rect x="44" y="286" width="24" height="10" transform="rotate(-12 56 291)" />
              <rect x="96" y="292" width="20" height="9" transform="rotate(7 106 296)" />
              <rect x="150" y="284" width="26" height="10" transform="rotate(-5 163 289)" />
              <rect x="198" y="293" width="18" height="9" transform="rotate(14 207 297)" />
            </g>
            <path d="M0 304 h260" class="g-bed" />
            <rect x="0" y="306" width="260" height="24" class="g-rock" />

            <text x="130" y="122" class="g-shout">AI FIRST</text>
          </svg>

          <!-- ---- panel B: data first ---- -->
          <svg viewBox="0 0 260 330" role="img" aria-labelledby="gnd-b">
            <title id="gnd-b"
              >Cutaway of a small plain cottage above the waterline, sitting on
              a wide column of stacked stone blocks that reaches the rock on
              the seabed.</title
            >
            <rect x="0" y="0" width="260" height="150" class="g-sky" />
            <rect x="0" y="150" width="260" height="180" class="g-water" />

            <!-- the cottage -->
            <rect x="100" y="100" width="62" height="50" class="g-wall" />
            <path d="M92 100 L131 70 L170 100 Z" class="g-roof" />
            <rect x="148" y="72" width="10" height="16" class="g-wall" />
            <g class="g-win">
              <rect x="110" y="112" width="14" height="14" />
              <rect x="138" y="112" width="14" height="14" />
            </g>
            <!-- a little planting, because the small house is the happy one -->
            <g class="g-shrub">
              <circle cx="88" cy="143" r="7" />
              <circle cx="95" cy="147" r="5" />
              <circle cx="176" cy="141" r="8" />
              <circle cx="167" cy="146" r="5" />
            </g>

            <!-- the pier: stacked courses, staggered joints, on rock -->
            <g class="g-block">
              <rect x="86" y="150" width="90" height="154" />
              <path d="M86 176 h90 M86 202 h90 M86 228 h90 M86 254 h90 M86 280 h90" class="g-joint" />
              <path
                d="M116 150 v26 M146 150 v26 M101 176 v26 M131 176 v26 M161 176 v26 M116 202 v26 M146 202 v26 M101 228 v26 M131 228 v26 M161 228 v26 M116 254 v26 M146 254 v26 M101 280 v24 M131 280 v24 M161 280 v24"
                class="g-joint"
              />
            </g>
            <path d="M0 304 h260" class="g-bed" />
            <rect x="0" y="306" width="260" height="24" class="g-rock" />

            <text x="130" y="248" class="g-shout">DATA FIRST</text>
          </svg>
        </div>
        <figcaption>
          Drawn here rather than reposted. The joke has been going around
          without an author attached to it, and it is too good to pass on
          unsigned.
        </figcaption>
      </figure>
    </section>

    <!-- ============ 01 ============ -->
    <section class="gnd-card">
      <p class="gnd-num">01 &nbsp;&middot;&nbsp; Above and below</p>
      <h2>What the demo leaves out.</h2>
      <p>
        A launch video shows the agent answering a question in plain English.
        It does not show which table the answer came from, which rows were
        excluded, or who decided that a canceled trial still counts as a
        customer. You need those answers before you can judge whether to trust
        the result.
      </p>
      <p>
        The drawing exaggerates the problem, but the distinction is useful: a
        working demo tells you little about the data supporting it.
      </p>
    </section>

    <!-- ============ 02 ============ -->
    <section class="gnd-card">
      <p class="gnd-num">02 &nbsp;&middot;&nbsp; The rented part</p>
      <h2>Other teams can use the same model.</h2>
      <p>
        Choosing and evaluating a gen AI model matters. But access to that model
        does not give it the knowledge your team has built up about the business.
      </p>
      <p>
        Consider a definition of <em>active subscriber</em> that survived three
        arguments with finance, a join graph checked for fan-out, and a record
        of which tables are safe to trust before 9am. Making that knowledge
        available to an agent takes work within your own team.
      </p>
      <p class="gnd-note">
        An agent can carry a mistaken definition through several tool calls.
        A later step may look reasonable even though it depends on an earlier
        query that counted the wrong customers.
      </p>
    </section>

    <!-- ============ 03 ============ -->
    <section class="gnd-card">
      <p class="gnd-num">03 &nbsp;&middot;&nbsp; The context checklist</p>
      <h2>What &ldquo;quality context&rdquo; means when you have to build it.</h2>
      <p>
        For an agent querying a warehouse, I would check the following. Having
        staging, intermediate, and mart models helps, but the agent still needs
        to know how to use them.
      </p>
      <ol class="gnd-spec">
        <li>
          <span class="sp-q">Meaning</span>
          <span class="sp-d"
            >What the business means by subscriber, churn, and month, including
            the exclusions.</span
          >
        </li>
        <li>
          <span class="sp-q">Grain</span>
          <span class="sp-d"
            >What one row represents in each fact table. Confusing orders with
            order items, for example, changes what a count means.</span
          >
        </li>
        <li>
          <span class="sp-q">Joins</span>
          <span class="sp-d"
            >Which join paths are valid, and which can duplicate revenue across
            marketing events.</span
          >
        </li>
        <li>
          <span class="sp-q">Freshness</span>
          <span class="sp-d"
            >When each table last landed, exposed to the agent rather than
            buried in an orchestration log.</span
          >
        </li>
        <li>
          <span class="sp-q">Grants</span>
          <span class="sp-d"
            >Who the agent is acting for, and what that person is allowed to
            see. Check whether the service account has broader permissions.</span
          >
        </li>
        <li>
          <span class="sp-q">Contracts</span>
          <span class="sp-d"
            >What each interface promises: the columns, the types, the
            nullability, the grain, and who gets paged when the promise
            breaks.</span
          >
        </li>
        <li>
          <span class="sp-q">Tests</span>
          <span class="sp-d"
            >Checks for uniqueness, referential integrity, accepted values,
            and freshness, with failures investigated before downstream use.</span
          >
        </li>
        <li>
          <span class="sp-q">Provenance</span>
          <span class="sp-d"
            >Who reviewed the definitions and checks, when they did so, and
            what has changed since.</span
          >
        </li>
      </ol>
      <p class="gnd-note">
        Set limits on what the agent can do as well: read-only credentials, row
        caps, a fixed set of exposed topics, and no path that bypasses the
        layer. These controls limit access and resource use; they do not
        establish that an answer is correct.
      </p>
      <p class="gnd-note">
        A model upgrade will not settle your subscriber definition or assign
        someone to investigate a failed data check.
      </p>
    </section>

    <!-- ============ 04 ============ -->
    <section class="gnd-card">
      <p class="gnd-num">04 &nbsp;&middot;&nbsp; Warehouse maintenance</p>
      <h2>The warehouse still needs the same care.</h2>
      <p>
        In an undocumented warehouse, two tables can look safe to join even
        when they represent different things. Analysts run into this too. An
        agent needs enough information to distinguish a valid join from one
        that merely runs.
      </p>
      <p>
        The usual analytics engineering work helps here: tests that fail when a
        key stops being unique, consistent naming and casting in staging, and
        fact and dimension models with a stated grain.
      </p>
      <p class="gnd-note">
        Keep the reasons for those choices too: why revenue excludes credits,
        why the trial table is snapshotted nightly, and which trade-off the
        team accepted. Write them down while the reasoning is still fresh.
      </p>
      <p>
        Prompt instructions and retrieval can help an agent find this information.
        Someone still has to maintain it.
      </p>
    </section>

    <!-- ============ 05 ============ -->
    <section class="gnd-card">
      <p class="gnd-num">05 &nbsp;&middot;&nbsp; The new failure mode</p>
      <h2>A fluent answer can make an error harder to notice.</h2>
      <p>
        A broken join can go unnoticed in a dashboard. Sometimes a colleague
        catches it because they know roughly what the number should be.
      </p>
      <p>
        An agent can turn that same number into a fluent recommendation to shift
        a budget. If the reader cannot inspect the query or recognize an
        implausible result, the explanation may make the error more persuasive.
      </p>
      <p>
        Where a workflow removes human review, more of the checking needs to
        happen before the answer is delivered. Shared definitions and tested
        models become especially useful there.
      </p>
    </section>

    <!-- ============ 06 ============ -->
    <section class="gnd-card">
      <p class="gnd-num">06 &nbsp;&middot;&nbsp; The translation problem</p>
      <h2>An ordinary question leaves definitions open.</h2>
      <p>
        We routinely leave details unstated when talking to colleagues. That
        works when we share the same assumptions.
      </p>
      <p>
        &ldquo;How many active customers do we have in France?&rdquo; is a
        perfectly good English sentence and an underspecified query. Active
        when, by which signal? A customer at the account level or the seat
        level? France by billing address, by IP, or by the sales territory
        that owns the account? A colleague may know the convention, or know
        whom to ask.
      </p>
      <p>
        Without those definitions, the same question can produce two defensible
        queries with different results. The reader needs to know which
        interpretation was used.
      </p>
      <p class="gnd-note">
        The semantic layer records agreed definitions so the agent can reuse
        them. It still needs to choose the right metric and filters, and ask
        for clarification when the question is ambiguous.
      </p>
    </section>

    <!-- ============ 07 ============ -->
    <section class="gnd-card">
      <p class="gnd-num">07 &nbsp;&middot;&nbsp; The quiet side effect</p>
      <h2>Another reason to maintain the semantic layer.</h2>
      <p>
        An experienced analyst can compensate for gaps in a model because they
        know the business. That knowledge is difficult to share with a new
        colleague, let alone an agent, unless it is recorded.
      </p>
      <p>
        Fields such as <code>ai_context</code> give teams a place to record
        guidance alongside model definitions. Use them to explain exclusions,
        preferred join paths, and questions a metric should not answer.
      </p>
      <p>
        Include that guidance in review. A technically valid definition can
        still leave its intended use unclear.
      </p>
      <p>
        I use the established term <em>semantic layer</em>, but prefer
        <strong>data context layer</strong>. It better describes what I want
        from it: enough context for a person or an agent to interpret a
        number correctly.
      </p>
      <p class="gnd-note">
        My proposed ownership split is for analysts to define meaning and
        engineers to validate the implementation, with both reviewing before
        publication. I explain the reasoning in
        <a
          href="https://github.com/aleberriz/intelligence-layer/blob/main/semantic-layer/frameworks/ownership-model.md"
          >the semantic layer ownership model</a
        >.
      </p>
    </section>

    <!-- ============ 08 ============ -->
    <section class="gnd-card">
      <p class="gnd-num">08 &nbsp;&middot;&nbsp; From prototype to production</p>
      <h2>Start with a limited prototype.</h2>
      <p>
        A prototype gives stakeholders something concrete to react to and helps
        uncover disagreements about definitions. Keep it in a sandbox while
        you work those out, with a separate review before production use.
      </p>
      <ol class="gnd-spec gnd-spec--steps">
        <li>
          <span class="sp-q">Write down the ten questions</span>
          <span class="sp-d"
            >Start with ten questions the business asks regularly.</span
          >
        </li>
        <li>
          <span class="sp-q">Prototype in a sandbox, fast and visibly</span>
          <span class="sp-d"
            >Use read-only access and resource limits, and keep the demo
            endpoint separate from operational workflows. Otherwise people
            may start relying on it before it has been reviewed.</span
          >
        </li>
        <li>
          <span class="sp-q">Define every term the prototype exposed</span>
          <span class="sp-d"
            >Record definitions and exclusions in the layer. Use disagreements
            about the demo answers to find what still needs clarification.</span
          >
        </li>
        <li>
          <span class="sp-q">Put a name on each definition</span>
          <span class="sp-d"
            >One person who answers for the meaning, one who answers for the
            math, and no publishing without both.</span
          >
        </li>
        <li>
          <span class="sp-q">Review the implementation before production</span>
          <span class="sp-d"
            >Have the analytics and data engineers use the prototype to specify
            what the production models need to do. Rework the implementation
            to fit the existing codebase, with its tests and access controls.</span
          >
        </li>
        <li>
          <span class="sp-q">Let the agent answer the ten</span>
          <span class="sp-d"
            >Investigate wrong answers and record the cause, whether it is a
            definition, a query, stale data, or a misread question.</span
          >
        </li>
        <li>
          <span class="sp-q">Release the reviewed scope</span>
          <span class="sp-d"
            >Start with the questions you have checked. Expand coverage as
            the supporting definitions and models are ready.</span
          >
        </li>
      </ol>
    </section>

    <!-- ============ CLOSER ============ -->
    <section class="gnd-close">
      <p class="gnd-num">The job</p>
      <p class="gnd-shout">Make the data work part of the plan.</p>
      <p>
        When planning an agent demo, include time to review the definitions,
        joins, permissions, and wrong answers it exposes. That work will also
        help the people already using the warehouse.
      </p>
    </section>

    <!-- ============ TITLE BLOCK ============ -->
    <footer class="gnd-foot">
      <dl class="gnd-block">
        <div><dt>Note</dt><dd>Gen AI without data</dd></div>
        <div><dt>Field</dt><dd>AI and the semantic layer</dd></div>
        <div><dt>Drawn by</dt><dd>Alejandro Berrizbeitia</dd></div>
        <div><dt>Founding</dt><dd>On rock</dd></div>
      </dl>
      <p class="gnd-foot-h">Go deeper</p>
      <p>
        <a
          href="https://github.com/aleberriz/intelligence-layer/blob/main/semantic-layer/frameworks/ownership-model.md"
          >Semantic layer ownership model</a
        >. Who defines meaning, who validates correctness, and why neither
        should publish alone once an agent is reading the output.
      </p>
      <p>
        <a href="https://open-semantic-interchange.org/">Open Semantic Interchange</a>.
        The cross-vendor spec where <code>ai_context</code> lives.
      </p>
      <p class="gnd-back">
        <a href="/field-notes/">← All field notes</a>
        <a href="/">aleberriz.com</a>
      </p>
    </footer>
  </div>
</div>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Archivo+Black&family=Archivo:wght@400;500;600&display=swap');

  .gnd {
    --sky: #12303f;
    --deep: #041820;
    --sea: #0a3a47;
    --foam: #7fd8d2;
    --amber: #f6b93b;
    --coral: #ff6f59;
    --body: #cfe0e4;
    --dim: #7d97a1;
    --rule: rgba(127, 216, 210, 0.16);

    min-height: 100vh;
    color: var(--body);
    /* the waterline runs down the whole page, not just the drawing */
    background:
      linear-gradient(180deg, #0d2532 0 240px, var(--deep) 560px),
      var(--deep);
    font-family: Archivo, Inter, ui-sans-serif, system-ui, sans-serif;
    font-size: 1.0625rem;
    line-height: 1.65;
  }

  .gnd-inner {
    max-width: 42rem;
    margin: 0 auto;
    padding: 0 1.25rem 4rem;
  }

  .gnd p { margin: 0 0 1.05rem; }
  .gnd a { color: var(--foam); text-underline-offset: 3px; }
  .gnd a:hover { color: var(--amber); }
  .gnd code {
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: 0.9em;
    color: var(--amber);
  }

  .gnd-kicker,
  .gnd-num,
  .gnd-foot-h,
  .gnd-back,
  .gnd-block dt {
    font-family: Archivo, Inter, sans-serif;
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--dim);
  }

  /* ---------- hero ---------- */
  .gnd-hero { padding: 4rem 0 3rem; }
  .gnd-hero h1 {
    margin: 1.1rem 0 1.25rem;
    font-family: 'Archivo Black', Archivo, Inter, sans-serif;
    font-size: clamp(2.2rem, 9.5vw, 3.5rem);
    line-height: 1.02;
    letter-spacing: -0.02em;
    color: #fff;
    text-wrap: balance;
  }
  .gnd-dek {
    font-size: 1.15rem;
    color: #a8c2c9;
    max-width: 38ch;
  }

  /* ---------- the drawing ---------- */
  .gnd-meme { margin: 2.5rem 0 0; }
  .gnd-panels {
    display: flex;
    flex-wrap: wrap;
    gap: 2px;
    background: rgba(127, 216, 210, 0.22);
    border: 2px solid rgba(127, 216, 210, 0.22);
    border-radius: 3px;
    overflow: hidden;
  }
  .gnd-panels svg {
    flex: 1 1 210px;
    width: 100%;
    height: auto;
    display: block;
  }
  .gnd-meme figcaption {
    margin-top: 0.9rem;
    font-size: 0.88rem;
    color: var(--dim);
  }

  .g-sky { fill: #1d4257; }
  .g-water { fill: #0a3546; }
  .g-rock { fill: #05141b; }
  .g-bed {
    stroke: #7fd8d2;
    stroke-opacity: 0.45;
    stroke-width: 2;
    fill: none;
  }
  .g-wall { fill: #e8ddc6; stroke: #05141b; stroke-width: 2; }
  .g-trim { fill: #c9b998; stroke: #05141b; stroke-width: 2; }
  .g-roof { fill: #e2704a; stroke: #05141b; stroke-width: 2; }
  .g-pool { fill: #56c2d6; stroke: #05141b; stroke-width: 2; }
  .g-win rect { fill: #1d4257; stroke: #05141b; stroke-width: 1.5; }
  .g-para { fill: #e2704a; stroke: #05141b; stroke-width: 2; stroke-linejoin: round; }
  .g-thin { fill: none; stroke: #05141b; stroke-width: 2; stroke-linecap: round; }
  .g-crack { fill: none; stroke: #c0392b; stroke-width: 2; stroke-linejoin: round; }
  .g-pile path {
    fill: none;
    stroke: #b98a4e;
    stroke-width: 7;
    stroke-linecap: round;
  }
  .g-shrub circle { fill: #4f8f6a; stroke: #05141b; stroke-width: 2; }
  .g-rubble rect { fill: #9aa7ad; stroke: #05141b; stroke-width: 1.5; }
  .g-block rect { fill: #9fb0b6; stroke: #05141b; stroke-width: 2; }
  .g-joint {
    fill: none;
    stroke: #05141b;
    stroke-width: 1.5;
    stroke-opacity: 0.55;
  }
  .g-shout {
    font-family: 'Archivo Black', Archivo, Impact, sans-serif;
    font-size: 30px;
    text-anchor: middle;
    fill: #fff;
    stroke: #05141b;
    stroke-width: 6;
    paint-order: stroke fill;
    letter-spacing: 0.01em;
  }

  /* ---------- sections ---------- */
  .gnd-card,
  .gnd-close {
    padding: 3rem 0;
    border-top: 1px solid var(--rule);
  }
  .gnd-card h2,
  .gnd-close h2 {
    margin: 0.55rem 0 1.2rem;
    font-family: 'Archivo Black', Archivo, Inter, sans-serif;
    font-size: clamp(1.5rem, 6.2vw, 2.05rem);
    line-height: 1.14;
    letter-spacing: -0.015em;
    color: #fff;
    text-wrap: balance;
  }

  .gnd-note {
    margin-top: 1.5rem;
    padding: 0.9rem 0 0.9rem 1.1rem;
    border-left: 3px solid var(--amber);
    font-size: 0.99rem;
    color: #b6cbd1;
  }

  /* ---------- numbered specs ---------- */
  .gnd-spec {
    margin: 1.5rem 0 0;
    padding: 0;
    list-style: none;
    counter-reset: pile;
  }
  .gnd-spec li {
    position: relative;
    padding: 0.85rem 0 0.85rem 2.5rem;
    border-top: 1px solid var(--rule);
  }
  .gnd-spec li::before {
    counter-increment: pile;
    content: counter(pile, decimal-leading-zero);
    position: absolute;
    left: 0;
    top: 1rem;
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    color: var(--foam);
  }
  .sp-q {
    display: block;
    font-family: 'Archivo Black', Archivo, sans-serif;
    font-size: 1.02rem;
    color: #fff;
  }
  .sp-d {
    display: block;
    margin-top: 0.3rem;
    font-size: 0.97rem;
    color: #b6cbd1;
  }
  .gnd-spec--steps li::before { color: var(--amber); }

  /* ---------- closer ---------- */
  .gnd-shout {
    font-family: 'Archivo Black', Archivo, Inter, sans-serif;
    font-size: clamp(1.8rem, 8vw, 2.6rem);
    line-height: 1.06;
    letter-spacing: -0.02em;
    color: var(--amber);
    text-wrap: balance;
  }

  /* ---------- title block ---------- */
  .gnd-foot {
    padding: 2.5rem 0 0;
    border-top: 1px solid var(--rule);
    font-size: 0.95rem;
  }
  .gnd-block {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1px;
    margin: 0 0 2rem;
    background: var(--rule);
    border: 1px solid var(--rule);
  }
  .gnd-block > div { padding: 0.7rem 0.85rem; background: var(--deep); }
  .gnd-block dt { margin-bottom: 0.15rem; }
  .gnd-block dd { margin: 0; color: #fff; }
  .gnd-foot-h { margin-bottom: 0.6rem; color: var(--foam); }
  .gnd-back { display: flex; gap: 1.5rem; padding-top: 1rem; }

  @media (min-width: 40rem) {
    .gnd-block { grid-template-columns: repeat(4, minmax(0, 1fr)); }
  }

  /* ---------- present mode ---------- */
  :global(html.present) .gnd { font-size: 1.2rem; }
</style>
